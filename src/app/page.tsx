import { Button } from "@/components/ui/button";

const Page = () => (
  <div className="flex min-h-screen items-center justify-center">
    <Button className="cursor-pointer">BetterAuth</Button>
  </div>
);

export default Page;
